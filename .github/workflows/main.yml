name: My First CI/CD Pipeline

on:
  push:
    branches:
      - main # или master, в зависимости от имени основной ветки вашего репозитория
  pull_request:
    branches:
      - main # или master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Build Step
        run: echo "Building the application..."

      - name: Run Test Step
        run: echo "Running tests..."
